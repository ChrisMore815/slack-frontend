{
    "compilerOptions": {
        "baseUrl": ".", // The base directory to resolve non-relative module names
        "paths": {
            "src/*": ["./src/*"] // Maps the src/* path to the ./src/* directory
        }
    },
    "include": [
        "src/**/*" // Include all files in the src directory
    ]
}
